trigger:
  - master

variables:
  # UPDATE THESE TWO NAMES BELOW
  dockerRegistryServiceConnection: "REPLACE_WITH_YOUR_ACR_CONN_NAME"
  kubernetesServiceConnection: "REPLACE_WITH_YOUR_AKS_CONN_NAME"

  acrHost: "threetierregistry.azurecr.io"
  tag: "$(Build.BuildId)"

pool:
  vmImage: "ubuntu-latest"

stages:
  - stage: CI_CD
    jobs:
      - job: Build_And_Deploy
        steps:
          - script: |
              echo "Running Unit Tests..."
              # Add your test commands here
            displayName: "Step 1: Unit Test"

          - task: Docker@2
            displayName: "Step 2: Build and Push"
            inputs:
              containerRegistry: $(dockerRegistryServiceConnection)
              repository: "threetier-app"
              command: "buildAndPush"
              tags: |
                $(tag)

          - script: |
              # Simple scan command
              docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
              aquasec/trivy image $(acrHost)/threetier-app:$(tag)
            displayName: "Step 3: Security Scan"

          - task: KubernetesManifest@1
            displayName: "Step 4: Deploy to AKS"
            inputs:
              action: "deploy"
              connectionType: "kubernetesServiceConnection"
              kubernetesServiceConnection: $(kubernetesServiceConnection)
              namespace: "default"
              manifests: |
                $(System.DefaultWorkingDirectory)/k8s-spec.yaml
              containers: |
                $(acrHost)/threetier-app:$(tag)
